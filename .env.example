# ============================================================================
# EjercicioCopilot - Environment Variables Example
# ============================================================================
#
# Copia este archivo a .env para configurar variables de entorno
# en docker-compose.
#
# Uso:
#   cp .env.example .env
#   # Editar .env con tus valores
#   docker-compose --env-file .env up -d
#
# Nota: Los valores aquí son ejemplos. Cambiar para producción.
#

# ============================================================================
# APLICACIÓN
# ============================================================================

APP_NAME=EjercicioCopilot
APP_VERSION=1.0.0
APP_DESCRIPTION="Generador de Excusas Tech"

# ============================================================================
# JAVA / JVM
# ============================================================================

# Opciones de JVM (memoria, GC, etc.)
JAVA_OPTS=-Xmx512m -Xms256m -XX:+UseG1GC

# Perfil activo de Spring Boot
SPRING_PROFILES_ACTIVE=docker

# ============================================================================
# SERVIDOR
# ============================================================================

# Puerto donde escucha la aplicación
SERVER_PORT=8080

# Contexto raíz
SERVER_SERVLET_CONTEXT_PATH=/

# Timeout de graceful shutdown (segundos)
SPRING_LIFECYCLE_TIMEOUT_PER_SHUTDOWN_PHASE=30s

# ============================================================================
# BASE DE DATOS
# ============================================================================

# H2 Database (desarrollo)
SPRING_DATASOURCE_URL=jdbc:h2:mem:testdb
SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.h2.Driver
SPRING_DATASOURCE_USERNAME=sa
SPRING_DATASOURCE_PASSWORD=

# H2 Console
SPRING_H2_CONSOLE_ENABLED=true
SPRING_H2_CONSOLE_PATH=/h2-console

# PostgreSQL (para producción - descomenta si usas PostgreSQL)
# SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/ejerciciocopilot
# SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.postgresql.Driver
# SPRING_DATASOURCE_USERNAME=appuser
# SPRING_DATASOURCE_PASSWORD=changeme
# SPRING_JPA_DATABASE_PLATFORM=org.hibernate.dialect.PostgreSQLDialect

# DDL Auto (create-drop para dev, validate para prod)
SPRING_JPA_HIBERNATE_DDL_AUTO=create-drop

# ============================================================================
# LOGGING
# ============================================================================

# Nivel de log global
LOGGING_LEVEL_ROOT=INFO

# Nivel de log para la aplicación
LOGGING_LEVEL_COM_EJERCICIOCOPILOT=DEBUG

# Nivel de log para controllers
LOGGING_LEVEL_COM_EJERCICIOCOPILOT_CONTROLLER=DEBUG

# Nivel de log para services
LOGGING_LEVEL_COM_EJERCICIOCOPILOT_SERVICE=DEBUG

# Nivel de log para Hibernate SQL
LOGGING_LEVEL_ORG_HIBERNATE_SQL=DEBUG

# ============================================================================
# ACTUATOR / MONITORING
# ============================================================================

# Endpoints de actuator expuestos
MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,info,metrics,env

# Health check con detalles
MANAGEMENT_ENDPOINT_HEALTH_SHOW_DETAILS=always

# ============================================================================
# DOCKER COMPOSE - SERVICIOS
# ============================================================================

# PostgreSQL (si está habilitado en docker-compose.yml)
DB_NAME=ejerciciocopilot
DB_USER=appuser
DB_PASSWORD=changeme
DB_PORT=5432

# ============================================================================
# PRODUCCIÓN (valores ejemplares, NO usar en desarrollo)
# ============================================================================

# Para producción, cambiar a:
# SERVER_SHUTDOWN=graceful
# SPRING_JPA_HIBERNATE_DDL_AUTO=validate
# LOGGING_LEVEL_ROOT=WARN
# SPRING_DATASOURCE_URL=jdbc:postgresql://prod-db-host:5432/app
# JAVA_OPTS=-Xmx2g -Xms1g -XX:+UseG1GC -XX:MaxGCPauseMillis=200

# ============================================================================
# NOTAS
# ============================================================================
#
# 1. NUNCA commitear el archivo .env (está en .gitignore)
# 2. Usar .env para desarrollo/testing local
# 3. Para producción, usar secrets en Kubernetes o variables del sistema
# 4. Cambiar contraseñas por defecto (DB_PASSWORD, etc.)
# 5. Usar valores seguros para JAVA_OPTS en producción
#
# ============================================================================
